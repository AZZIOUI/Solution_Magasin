@model Solution_Magasin.ViewModels.PaymentViewModel
@{
    ViewData["Title"] = "Paiement";
}

<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-0 shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="bi bi-credit-card"></i> Paiement sécurisé</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info mb-4">
                        <i class="bi bi-info-circle"></i>
                        <strong>Note:</strong> Ceci est une simulation de paiement. Aucune transaction réelle ne sera effectuée.
                    </div>

                    <div class="mb-4 p-3 bg-light rounded">
                        <div class="d-flex justify-content-between">
                            <span>Montant à payer:</span>
                            <strong class="text-primary fs-4">@Model.TotalAmount.ToString("N2") DH</strong>
                        </div>
                    </div>

                    <form asp-action="ProcessPayment" method="post">
                        <input type="hidden" asp-for="OrderId" />
                        <input type="hidden" asp-for="TotalAmount" />

                        <div class="mb-3">
                            <label asp-for="CardHolderName" class="form-label"></label>
                            <input asp-for="CardHolderName" class="form-control" placeholder="Jean Dupont" required />
                            <span asp-validation-for="CardHolderName" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="CardNumber" class="form-label"></label>
                            <input asp-for="CardNumber" class="form-control" placeholder="1234 5678 9012 3456" maxlength="19" required />
                            <span asp-validation-for="CardNumber" class="text-danger"></span>
                            <small class="form-text text-muted">Exemple: 4111111111111111</small>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="ExpiryDate" class="form-label"></label>
                                <input asp-for="ExpiryDate" class="form-control" placeholder="MM/AA" maxlength="5" required />
                                <span asp-validation-for="ExpiryDate" class="text-danger"></span>
                                <small class="form-text text-muted">Exemple: 12/25</small>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="CVV" class="form-label"></label>
                                <input asp-for="CVV" class="form-control" placeholder="123" maxlength="4" required />
                                <span asp-validation-for="CVV" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="alert alert-warning">
                            <i class="bi bi-shield-check"></i>
                            Vos informations sont sécurisées et ne seront pas stockées.
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="bi bi-lock-fill"></i> Valider le paiement
                            </button>
                            <a asp-action="Checkout" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Retour
                            </a>
                        </div>
                    </form>
                </div>
                <div class="card-footer bg-light text-center">
                    <small class="text-muted">
                        <i class="bi bi-shield-lock"></i> Paiement 100% sécurisé
                    </small>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script>
        // Format card number input
        document.querySelector('input[name="CardNumber"]').addEventListener('input', function(e) {
            let value = e.target.value.replace(/\s/g, '');
            let formattedValue = value.match(/.{1,4}/g)?.join(' ') || value;
            e.target.value = formattedValue;
        });

        // Format expiry date
        document.querySelector('input[name="ExpiryDate"]').addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            if (value.length >= 2) {
                value = value.substring(0, 2) + '/' + value.substring(2, 4);
            }
            e.target.value = value;
        });
    </script>
}
